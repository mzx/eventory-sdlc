import { Typography } from '@mui/material';

/**
 * Stub — the photo → AI draft → confirm intake flow arrives in EVT-11. This
 * placeholder exists so the AppBar "Add item" action and the empty-state CTA
 * on ItemsPage have a route to land on.
 */
export function IntakePage() {
  return (
    <Typography variant="body1" color="text.secondary">
      Photo intake flow lands in EVT-11.
    </Typography>
  );
}
